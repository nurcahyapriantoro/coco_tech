# ──────────────────────────────────────────────
# CocoTech — Environment Variables Example
# Copy this file to .env and fill in real values
# NEVER commit .env to git
# ──────────────────────────────────────────────

# API base URL (used by frontend proxy in dev)
# For production, set this in Digital Ocean App Platform environment variables
VITE_API_URL=https://api.yourdomain.com
